local Lib = loadstring(game:HttpGet("https://raw.githubusercontent.com/TimkaZXC/AQGJYRVBTXOMHFEIWCKPDLUSNZ/refs/heads/main/Library"))()
local Hub = Lib.Hub("[🎄]Test")
local Main = Hub:Section("[✨]Main")
Main:Title("[✴️]Manual", Color3.fromRGB(200, 100, 200))
Main:Toggle("[✴️]Slash", Color3.fromRGB(0,255, 255), function()
local plr = game.Players.LocalPlayer
local plrG = plr.PlayerGui
local Active = plrG.InterfaceGui.Input
Active.Attack.MouseButton1Click:Connect(function()
local char = plr.Character
local swordF = char:FindFirstChild("SwordFolder")
for _, event in pairs(swordF:GetDescendants()) do
if event:IsA("RemoteEvent") then
local args = {
    [1] = "Start",
    [2] = 1
}

event:FireServer(unpack(args))
task.wait(n)
local args = {
    [1] = "Stop",
    [2] = 1
}

event:FireServer(unpack(args))
end
end
end)
end)


Main:Title("[✴️]Auto", Color3.fromRGB(200, 100, 200))
Main:Change("[✴️]Slash (CD)", 2, 4, Color3.fromRGB(0,255, 255), function(n)
local plr = game.Players.LocalPlayer
local char = plr.Character
local swordF = char:FindFirstChild("SwordFolder")
for _, event in pairs(swordF:GetDescendants()) do
if event:IsA("RemoteEvent") then
local args = {
    [1] = "Start",
    [2] = 1
}

event:FireServer(unpack(args))
task.wait(n)
local args = {
    [1] = "Stop",
    [2] = 1
}

event:FireServer(unpack(args))
end
return
end
end)

Main:Change("[✴️]SkillA (CD)", 2, 4, Color3.fromRGB(0,255, 255), function(n)
local args = {
    [1] = "SkillA",
    [2] = "Get"
}

game:GetService("ReplicatedStorage").RemoteEvent.Skill:FireServer(unpack(args))
local args = {
    [1] = "SkillA",
    [2] = "Active"
}

game:GetService("ReplicatedStorage").RemoteEvent.Skill:FireServer(unpack(args))
wait(n)
end)

Main:Change("[✴️]SkillB (CD)", 2, 4, Color3.fromRGB(0,255, 255), function(n)
local args = {
    [1] = "SkillB",
    [2] = "Get"
}

game:GetService("ReplicatedStorage").RemoteEvent.Skill:FireServer(unpack(args))
local args = {
    [1] = "SkillB",
    [2] = "Active"
}

game:GetService("ReplicatedStorage").RemoteEvent.Skill:FireServer(unpack(args))
wait(n)
end)

Main:Toggle("[✴️]Hit", Color3.fromRGB(0,255, 255), function(n)
local plr = game.Players.LocalPlayer
local char = plr.Character
local swordF = char:FindFirstChild("SwordFolder")
for _, event in pairs(swordF:GetDescendants()) do
if event:IsA("RemoteEvent") then
local args = {
    [1] = "Start",
    [2] = 0
}

event:FireServer(unpack(args))
end
end
return
end)

Main:Active("[✴️]UnHit", Color3.fromRGB(0,255, 255), function(n)
local plr = game.Players.LocalPlayer
local char = plr.Character
local swordF = char:FindFirstChild("SwordFolder")
for _, event in pairs(swordF:GetDescendants()) do
if event:IsA("RemoteEvent") then
local args = {
    [1] = "Stop",
    [2] = 0
}

event:FireServer(unpack(args))
end
end
return
end)

local Main = Hub:Section("[✨]Teleport")
Main:Title("[✨]Mobs", Color3.fromRGB(200, 100, 200))
Main:Toggle("[🧟]Green Slime LVL: 1", Color3.fromRGB(0,255, 255), function()
local plr = game.Players.LocalPlayer
local char = plr.Character
for _, enemy in pairs(workspace.Entity:GetChildren()) do
pcall(function()
if enemy.Name == "Green-Slime" then
while enemy.Humanoid.Health > 0 do
char.HumanoidRootPart.CFrame = enemy.HumanoidRootPart.CFrame * CFrame.new(0,-0.5,3)
wait()
end
end
end)
end
return
end)

Main:Toggle("[🧟]Metal Slime LVL: 1", Color3.fromRGB(0,255, 255), function()
local plr = game.Players.LocalPlayer
local char = plr.Character
for _, enemy in pairs(workspace.Entity:GetChildren()) do
pcall(function()
if enemy.Name == "Metal-Slime" then
while enemy.Humanoid.Health > 0 do
char.HumanoidRootPart.CFrame = enemy.HumanoidRootPart.CFrame * CFrame.new(0,-0.5,3)
wait()
end
end
end)
end
return
end)

Main:Toggle("[🧟]Amanitas LVL: 3", Color3.fromRGB(0,255, 255), function()
local plr = game.Players.LocalPlayer
local char = plr.Character
for _, enemy in pairs(workspace.Entity:GetChildren()) do
pcall(function()
if enemy.Name == "Amanita" then
while enemy.Humanoid.Health > 0 do
char.HumanoidRootPart.CFrame = enemy.HumanoidRootPart.CFrame * CFrame.new(0,-0.5,3)
wait()
end
end
end)
end
return
end)

Main:Toggle("[🧟]Red Slime LVL: 5", Color3.fromRGB(0,255, 255), function()
local plr = game.Players.LocalPlayer
local char = plr.Character
for _, enemy in pairs(workspace.Entity:GetChildren()) do
pcall(function()
if enemy.Name == "Red-Slime" then
while enemy.Humanoid.Health > 0 do
char.HumanoidRootPart.CFrame = enemy.HumanoidRootPart.CFrame * CFrame.new(0,-0.5,3)
wait()
end
end
end)
end
return
end)

Main:Toggle("[🧟]Hornet LVL: 7", Color3.fromRGB(0,255, 255), function()
local plr = game.Players.LocalPlayer
local char = plr.Character
for _, enemy in pairs(workspace.Entity:GetChildren()) do
pcall(function()
if enemy.Name == "Hornet" then
while enemy.Humanoid.Health > 0 do
char.HumanoidRootPart.CFrame = enemy.HumanoidRootPart.CFrame * CFrame.new(0,-0.5,3)
wait()
end
end
end)
end
return
end)

Main:Toggle("[🧟]Camo Hornet LVL: 9", Color3.fromRGB(0,255, 255), function()
local plr = game.Players.LocalPlayer
local char = plr.Character
for _, enemy in pairs(workspace.Entity:GetChildren()) do
pcall(function()
if enemy.Name == "Camo-Hornet" then
while enemy.Humanoid.Health > 0 do
char.HumanoidRootPart.CFrame = enemy.HumanoidRootPart.CFrame * CFrame.new(0,-0.5,3)
wait()
end
end
end)
end
return
end)

Main:Toggle("[🧟]Loiter LVL: 12", Color3.fromRGB(0,255, 255), function()
local plr = game.Players.LocalPlayer
local char = plr.Character
for _, enemy in pairs(workspace.Entity:GetChildren()) do
pcall(function()
if enemy.Name == "Loiter" then
while enemy.Humanoid.Health > 0 do
char.HumanoidRootPart.CFrame = enemy.HumanoidRootPart.CFrame * CFrame.new(0,-0.5,4)
wait()
end
end
end)
end
return
end)

Main:Toggle("[🧟]Loiter The Chief LVL: 20", Color3.fromRGB(0,255, 255), function()
local plr = game.Players.LocalPlayer
local char = plr.Character
for _, enemy in pairs(workspace.Entity:GetChildren()) do
pcall(function()
if enemy.Name == "Loiter-The-Chief" then
while enemy.Humanoid.Health > 0 do
char.HumanoidRootPart.CFrame = enemy.HumanoidRootPart.CFrame * CFrame.new(0,-0.5,4)
wait()
end
end
end)
end
return
end)

Main:Toggle("[🧟]Cave Scorpion LVL: 23", Color3.fromRGB(0,255, 255), function()
local plr = game.Players.LocalPlayer
local char = plr.Character
for _, enemy in pairs(workspace.Entity:GetChildren()) do
pcall(function()
if enemy.Name == "Cave-Scorpion" then
while enemy.Humanoid.Health > 0 do
char.HumanoidRootPart.CFrame = enemy.HumanoidRootPart.CFrame * CFrame.new(0,-0.5,6)
wait()
end
end
end)
end
return
end)

Main:Toggle("[🧟]Crystallized Scorpion LVL: 28", Color3.fromRGB(0,255, 255), function()
local plr = game.Players.LocalPlayer
local char = plr.Character
for _, enemy in pairs(workspace.Entity:GetChildren()) do
pcall(function()
if enemy.Name == "Crystallized-Scorpion" then
while enemy.Humanoid.Health > 0 do
char.HumanoidRootPart.CFrame = enemy.HumanoidRootPart.CFrame * CFrame.new(0,-0.5,6)
wait()
end
end
end)
end
return
end)

Main:Toggle("[🧟]Mini Snowman LVL: 30", Color3.fromRGB(0,255, 255), function()
local plr = game.Players.LocalPlayer
local char = plr.Character
for _, enemy in pairs(workspace.Entity:GetChildren()) do
pcall(function()
if enemy.Name == "Mini-Snowman" then
while enemy.Humanoid.Health > 0 do
char.HumanoidRootPart.CFrame = enemy.HumanoidRootPart.CFrame * CFrame.new(0,-0.5,6)
wait()
end
end
end)
end
return
end)

Main:Toggle("[🧟]Gaint Snowman LVL: 35", Color3.fromRGB(0,255, 255), function()
local plr = game.Players.LocalPlayer
local char = plr.Character
for _, enemy in pairs(workspace.Entity:GetChildren()) do
pcall(function()
if enemy.Name == "Gaint-Snowman" then
while enemy.Humanoid.Health > 0 do
char.HumanoidRootPart.CFrame = enemy.HumanoidRootPart.CFrame * CFrame.new(0,-0.5,6)
wait()
end
end
end)
end
return
end)

Main:Title("[✨]Event Mob", Color3.fromRGB(255, 155, 255))
Main:Toggle("[🧟]Apprentice Loiter LVL: 25", Color3.fromRGB(0,255, 255), function()
local plr = game.Players.LocalPlayer
local char = plr.Character
for _, enemy in pairs(workspace.Entity:GetChildren()) do
pcall(function()
if enemy.Name == "Apprentice-Loiter" then
while enemy.Humanoid.Health > 0 do
char.HumanoidRootPart.CFrame = enemy.HumanoidRootPart.CFrame * CFrame.new(0,-0.5,4)
wait()
end
end
end)
end
return
end)

Main:Toggle("[🧟]Loiter The Giftbringer LVL: 35", Color3.fromRGB(0,255, 255), function()
local plr = game.Players.LocalPlayer
local char = plr.Character
for _, enemy in pairs(workspace.Entity:GetChildren()) do
pcall(function()
if enemy.Name == "Loiter-The-Giftbringer" then
while enemy.Humanoid.Health > 0 do
char.HumanoidRootPart.CFrame = enemy.HumanoidRootPart.CFrame * CFrame.new(0,-0.5,4)
wait()
end
end
end)
end
return
end)

Main:Title("[✨]Chests", Color3.fromRGB(255, 155, 255))
Main:Toggle("[🧰]Mimic", Color3.fromRGB(0,255, 255), function()
local plr = game.Players.LocalPlayer
local char = plr.Character
for _, enemy in pairs(workspace.Entity:GetChildren()) do
pcall(function()
if enemy.Name == "Mimic" then
while enemy.Humanoid.Health > 0 do
char.HumanoidRootPart.CFrame = enemy.HumanoidRootPart.CFrame * CFrame.new(0,-0.5,4)
wait()
end
end
end)
end
return
end)

Main:Toggle("[🧰]GiftBox Mimic", Color3.fromRGB(0,255, 255), function()
local plr = game.Players.LocalPlayer
local char = plr.Character
for _, enemy in pairs(workspace.Entity:GetChildren()) do
pcall(function()
if enemy.Name == "GiftBox-Mimic" then
while enemy.Humanoid.Health > 0 do
char.HumanoidRootPart.CFrame = enemy.HumanoidRootPart.CFrame * CFrame.new(0,-0.5,4)
wait()
end
end
end)
end
return
end)

Main:Toggle("[🧰]Wooden", Color3.fromRGB(0,255, 255), function()
local plr = game.Players.LocalPlayer
local char = plr.Character
for _, enemy in pairs(workspace.Entity:GetChildren()) do
pcall(function()
if enemy.Name == "Wooden-Chest" then
while enemy.Humanoid.Health > 0 do
char.HumanoidRootPart.CFrame = enemy.HumanoidRootPart.CFrame * CFrame.new(0,-0.5,4)
wait()
end
end
end)
end
return
end)
